Day 7 Incrementing Counter

A simple UI that shows the incrementing number of followers, subscribers, and friends

## What I Learned

In JavaScript

1 Use of `getAttribute()` property

2 Use of `Math.ceil` to get the smallest integer greater than or equal to its numeric argument.