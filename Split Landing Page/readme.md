# Day 4 — Split Landing Page

When hovering over one side of the split screen, it expands to 75% of the screen width.

## What I Learned

In JavaScript:
- Using the `mouseenter` and `mouseleave` event types.
