Day 13 - Double Heart Click
A simple project that shows a heart emoji animation when the user double-clicks on an image or div. It also keeps track of the number of double clicks.

## What I Learned 

In JavaScript

1 How to create elements dynamically using `document.createElement()`.

2 How to position an element precisely using `getBoundingClientRect()` to get the coordinates of the clicked area inside a div.