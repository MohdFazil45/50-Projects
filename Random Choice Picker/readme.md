Day 6 - Random Choice Picker
A simple UI to select random choice

## What I Learned 

In HTML

1- Use of `textarea`

In JavaScript

1 Use of `input.split(',')` to splits the text by commas into an array.

2 Use of `.filter(tag => tag.trim() !== '')` to  removes empty tags.

3 Use of `.map(tag => tag.trim())` trims whitespace.