Day 3 :- Scroll Animation :-
A simple for scrolling animation where content comes to center in transition

## What I Learned

In JavaScript

=> How can i target height in display using window.innerHeight()

=> The method getBoundingClientRect() returns a DOMRect object that contains information about the size and position of an element relative to the viewport (the visible area of the browser window).

