Day 2 :- Blurring Loading :-


## What I Learned

In JavaScript

=> How can i do blur loading form 1 to 0 by 
function scale (number, inMin, inMax, outMin, outMax) {
    return (number - inMin) * (outMax - outMin) / (inMax - inMin) + outMin;
}

