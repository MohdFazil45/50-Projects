Day 10 - Kinetic CSS Loader 

A rotating triangle animation for a loading screen.

## What I Learned

In CSS 

1 How to make triangle using `::after` and `::before` pseudo-elements

2 How to creating rotating animation using CSS `@keyframes`